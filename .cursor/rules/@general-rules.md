---
description: This is a general rule that applies to all tasks and must be considered before submitting any pull request.
globs:
alwaysApply: true
---

- Do not use the type `any` for variables with `unknown` types. Always determine and specify the precise type required.
